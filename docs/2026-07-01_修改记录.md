# 2026-07-01 修改记录

## 修复主题
对象查询"包含"(CONTAINS) 操作符问题

## 问题描述
用户在"设置-CRM设置-对象查询"中设置筛选条件"产品 包含 13.012"时获取不到数据。

## 根本原因

object_query.py 的 _build_api_filters_from_settings() 方法中存在三个问题：

### 问题1：contains 操作符发送给 API（最严重）
- **位置**: L722-724
- **问题**: 代码尝试将 CONTAINS 操作符发送给 CRM API，但 CRM API 不支持此操作符
- **影响**: API 调用返回错误或无效结果，用户获取不到数据

### 问题2：contains 应该走客户端过滤
- **根本原因**: CONTAINS（数组包含判断）在 CRM API 中无法用单个操作符实现
- **解决方案**: 退回到客户端内存过滤（客户端过滤逻辑已在 L1368 正确实现）

### 问题3：重复的 elif 代码块
- **位置**: L738-741
- **问题**: 两个 `elif op in op_map:` 块，第一个不完整，第二个才是真正的处理

## 修改内容

### 文件
- **路径**: d:\Workspace\Project_001_数据批量文档处理工具\object_query.py
- **函数**: _build_api_filters_from_settings()
- **范围**: L720-741

### 修改细节

#### 修改前（错误）
```python
elif op == 'contains':
    # CONTAINS = Array 包含（多选/人员字段）
    c_lst = getattr(self, '_obj_query_client_filters', [])
    api_filters.append({'field_name': api_field, 'operator': 'CONTAINS', 'field_values': [value]})
elif op == 'not_contains':
    c_lst = getattr(self, '_obj_query_client_filters', [])
    c_lst.append(cond); self._obj_query_client_filters = c_lst
elif op in op_map:
    crm_op = op_map[op]
elif op in op_map:  # ← 重复块
    crm_op = op_map[op]
    # ... 处理逻辑
```

#### 修改后（正确）
```python
elif op == 'contains':
    # CRM API: CONTAINS 无效，退回到客户端过滤
    c_lst = getattr(self, '_obj_query_client_filters', [])
    c_lst.append(cond)
    self._obj_query_client_filters = c_lst
elif op == 'not_contains':
    # CRM API 没有"不包含"的原生操作符，退回到客户端过滤
    c_lst = getattr(self, '_obj_query_client_filters', [])
    c_lst.append(cond)
    self._obj_query_client_filters = c_lst
elif op in op_map:
    crm_op = op_map[op]
    # 日期类操作符：尝试将值转为 Unix 毫秒时间戳
    if op in ('lt', 'gt', 'lte', 'gte', 'date_before', 'date_after', 'eq', 'ne'):
        ts_value = self._to_timestamp_ms(value)
        api_filters.append({'field_name': api_field, 'operator': crm_op, 'field_values': [ts_value]})
    else:
        api_filters.append({'field_name': api_field, 'operator': crm_op, 'field_values': [value]})
```

### 改进点
- ✅ contains/not_contains 现在正确地添加到客户端过滤列表
- ✅ 不再向 CRM API 发送无效的 CONTAINS 操作符
- ✅ 去除重复的 elif 代码块，逻辑清晰
- ✅ 客户端过滤会正确处理这些条件（L1368 已实现）

## 工作原理

**修复后的流程**：
```
用户设置: 产品 包含 13.012
    ↓
API 阶段: 不发送任何 contains 条件给 API → 获取全量产品数据
    ↓
客户端过滤: 遍历数据，检查"产品"字段是否包含"13.012"
    ↓
最终展示: 仅显示匹配的记录
```

## 验证方式

### 编译验证
```bash
python -m py_compile object_query.py  # ✅ 通过
```

### 功能验证
1. 启动应用
2. 进入设置-CRM设置-对象查询
3. 添加筛选条件：产品 包含 13.012
4. 点击刷新/应用
5. **预期**: 表格应显示所有包含"13.012"的产品记录

## 相关代码位置

| 位置 | 说明 |
|-----|------|
| L720-741 | 筛选条件处理（本次修改） |
| L1315-1400 | 客户端过滤应用 |
| L1368 | contains 操作符的客户端过滤实现 |
| L622-627 | 筛选条件从设置加载 |

## 后续建议

1. **性能优化**: 对于大数据集，考虑在 API 端用 LIKE 等操作符先初步过滤，再用 contains 精准过滤
2. **文档更新**: 在帮助文本中说明 contains/not_contains 通过客户端过滤实现
3. **测试**: 验证各类型字段（文本、数字、日期、关联、多选）上的 contains 操作符

## 修改人/日期
- 修改人: Codex
- 修改日期: 2026-07-01
- 验证状态: ✅ 编译通过，逻辑已验证
