
| 2026-07-07 | [错误] | 对象查询 _fetch 线程函数缺少 _obj_query_mem_cache 初始化守护 | object_query.py 第1087行 self._obj_query_mem_cache[api_name] = self.obj_query_all_data 在_fetch()线程函数内直接使用属性，但线程启动时该属性可能尚未初始化 | 启动对象查询后报 AttributeError: 'MainFrame' object has no attribute '_obj_query_mem_cache' | _fetch() 函数内第1087行前加 if not hasattr(self, '_obj_query_mem_cache'): self._obj_query_mem_cache = {} | 线程函数中访问实例属性前必须先检查是否已初始化——主方法和线程函数之间的执行时序不可靠，不能假设主方法中的初始化一定在线程运行前完成 |

| 2026-07-07 | [错误] | 对象查询 `_get_crm_object_fields_cached` 传入多余关键字参数 | `object_query.py` 两处调用 `self._get_crm_object_fields_cached(api_name, use_api_fallback=False)` 但该方法来自 `order_to_contractMixin`，签名 `def _get_crm_object_fields_cached(self, api_name)` 不含 `use_api_fallback` | 对象查询页面打开时崩 `TypeError: unexpected keyword argument 'use_api_fallback'` | `object_query.py` 两处移除 `use_api_fallback=False` 参数 | 跨 mixin 调用方法时，参数必须与方法签名严格一致；不要假设所有 mixin 的实现版本都支持相同的关键字参数 |

### 可复用做法

**跨 Mixin 方法调用参数校验**：当从 mixin A 调用 mixin B 的方法时，不能假设 mixin B 的方法签名支持 mixin A 习惯传入的额外参数。需要先确认目标方法的实际签名。如果方法可能来自不同的 mixin，应使用 `inspect.signature` 校验或提供兼容包装。


| 2026-07-07 | [错误] | 设置→CRM设置筛选条件修改后不生效——缓存清除设错对象 | SettingsDialog._save_obj_query_settings() 中访问 self._obj_query_mem_cache，但该属性属于 MainFrame/object_queryMixin 而非 SettingsDialog。hasattr 返回 False，缓存永不被清除 | 用户改筛选条件后切到对象查询页面仍显示旧数据 | Project_zjt_3.5.8.py: 将 self._obj_query_mem_cache.pop() 改为通过 getattr(self, '_main_window') 获取 MainFrame 后再清除 | 缓存/标志属性不能跨实例假设。SettingsDialog 上的操作必须通过 _main_window 引用同步到 MainFrame |

### 可复用做法

**跨实例缓存操作**：当从 SettingsDialog 操作 MainFrame 的缓存时，必须通过 mw = getattr(self, '_main_window', None) 获取 MainFrame 引用，直接通过 self 访问不到 MainFrame 的属性。
